import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    imagem: {
      width: 50,
      height: 50,
      margin: 10,
      borderRadius: 30,
    },
    cabecalho: {
        flexDirection: 'row',
        alignItems: 'center',
    },
  });

  export default style;